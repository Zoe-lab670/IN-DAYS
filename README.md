# IN DAYS V35 P1 — 定向修复版

基于 V33 的定向修复版。本轮只处理：心情安全区、文字样式即时生效、贴纸推荐恢复；其余功能不改。

## P0 完成项

### 数据安全
- 新增“我的一隅 → 数据安全 → 导出完整备份”。
- 备份包含云端记录/本地记录、当天草稿、收藏、最近使用与当前视图偏好。
- 新增“恢复备份”，会先确认覆盖同日期记录，再写入当前账号。
- Supabase 恢复按批次 upsert，不修改数据库结构。
- 原 `config.js` 不包含在替换包中，继续保留现有 Supabase 配置。
- 页面关闭前继续写入当前草稿。

### 移动端编辑体验
- 编辑抽屉在窄屏使用 100dvh 全屏编辑。
- 头部、工具条、底部保存栏固定，编辑区独立滚动。
- 触控画布禁用页面级手势干扰，保留元素拖动/变换。
- 常用操作最小触控高度提升。
- 手机窄屏下备份操作也保持双列可用。

## 静态验收
- `node --check app.js` 通过。
- 原始贴纸 107 + 补充贴纸 126 = 233。
- 233 个贴纸 ID 唯一。
- 关键函数重复定义检查通过。
- index.html 的资源缓存版本统一为 V33。
- 替换包不包含 `config.js`，不创建 `stickers/` 文件夹。
- ZIP 完整性检查通过。

## 浏览器验收限制
当前工作环境的 Chromium headless 在启动阶段超时，因此无法在这里完成真实 GitHub Pages / Supabase / IME / 触控 E2E。该项不宣称通过。

## 替换规则
替换：`index.html`、`styles.css`、`app.js`、`README.md`。
保留原来的：`config.js`。

V33 是 P0 基线；P1（时间轴 / 模板版式 / 贴纸小场景）尚未开始。


## V34 定向修复
- 贴纸 / 照片 / 文字避让右上角心情区域，同时继续避让左上角日期。
- 心情标记位置与月历缩略图保持一致方向。
- 趣味字体点击后立即应用到当前画布文字，并写入草稿 / 云端保存流程。
- 恢复无文字时的默认贴纸推荐；保留节日与关键词推荐。
- 推荐区点击贴纸继续直接加入画布。
- 不包含 `config.js`。


### P1：时间轴 + 模板版式 + 贴纸小场景
- 时间轴强化为连续视觉时间线，并显示日期、心情与贴纸数量。
- 一日画布新增自由留白 / 主角居中 / 左右分栏 / 小画廊四种版式。
- 一日画布新增下午茶 / 工作一角 / 周末散步三个贴纸小场景。
- 这些功能复用现有数据保存结构，不新增配置文件。

## P1 修订版（返工）
- 贴纸小场景支持再次点击同一场景整组移除，并保留分组信息以便刷新后继续移除。
- 新增常驻“文字样式”面板：标题/正文切换、字体、字号、对齐可直接操作并立即更新画布。
- 月历缩略图提高标题与正文的可读字号，保留原有位置与整体构图。
- 扩大并重新定位右上心情安全区，文字/贴纸/照片避让范围同步收紧。
- 本次仅更新 `index.html`、`app.js`、`styles.css`、`README.md`；请继续保留原有 `config.js`。
